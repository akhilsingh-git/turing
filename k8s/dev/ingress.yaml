apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: turing
spec:
  rules:
    - http:
        paths:
          - backend:
              serviceName: clips-reco
              servicePort: 80
            path: /